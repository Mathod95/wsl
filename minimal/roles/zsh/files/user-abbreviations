# ansible
abbr "apbm"="ansible-playbook main.yml"
abbr "apbmv"="ansible-playbook main.yml --tags=versions"

# docker
abbr "dk"="docker"

# git
abbr "gl"="git pull"

# kubectl
abbr "kgno"="kubectl get nodes"
abbr "kgns"="kubectl get namespaces"
abbr "kgpons"="kubectl get pods % --namespace %"
abbr "k"="kubectl"
abbr "kctx"="kubectx"
abbr "kns"="kubens"

# cd
abbr ".."="cd .."
abbr "..."="cd ../.."

# ls
abbr "ll"="eza -la --icons"
abbr "ls"="eza --icons"
abbr "tree"="eza -T"
abbr "la"="eza -a --icons"

# shell
abbr "szshrc"="source ~/.zshrc"
abbr "zshrc"="vim ~/.zshrc"

# kind
abbr "kicc"="kind create cluster"
abbr "kiccn"="kind create cluster --name %"
abbr "kigc"="kind get clusters"
abbr "kign"="kind get nodes"
abbr "kidc"="kind delete cluster"
abbr "kidcn"="kind delete cluster --name %"
abbr "kidca"="kind delete clusters -A"
abbr "kigk"="kind get kubeconfig"
                                                 
# helm
abbr "h"="helm"
abbr "hd"="helm delete"
abbr "hi"="helm install"
abbr "hs"="helm status"
abbr "hl"="helm list"
abbr "hla"="helm list -A"
abbr "hu"="helm upgrade"
abbr "hui"="helm upgrade --install"
abbr "hun"="helm uninstall"

# opentofu
abbr "tt"="tofu"
abbr "tta"="tofu apply"
abbr "ttaa"="tofu apply -auto-approve"
abbr "ttc"="tofu console"
abbr "ttd"="tofu destroy"
abbr "ttd!"="tofu destroy -auto-approve"
abbr "ttf"="tofu fmt"
abbr "ttfr"="tofu fmt -recursive"
abbr "tti"="tofu init"
abbr "tto"="tofu output"
abbr "ttp"="tofu plan"
abbr "ttv"="tofu validate"
abbr "tts"="tofu state"
abbr "ttsh"="tofu show"
abbr "ttr"="tofu refresh"
abbr "ttt"="tofu test"
abbr "ttws"="tofu workspace"
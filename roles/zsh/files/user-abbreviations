# Ansible
abbr "apbzsh"="ansible-playbook main.yml --tags=zsh"

# Docker
abbr "dk"="docker"

# Git
abbr "gl"="git pull"

# Kubectl
abbr "kgno"="kubectl get nodes"
abbr "kgns"="kubectl get namespaces"
abbr "kgpons"="kubectl get pods % --namespace %"
abbr "k"="kubectl"
abbr "kctx"="kubectx"
abbr "kns"="kubens"